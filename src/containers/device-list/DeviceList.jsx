import React from 'react';
import styles from './DeviceList.module.css';

const DeviceList = () => {
  return <section className={styles.container}>DeviceList</section>;
};

export default DeviceList;
